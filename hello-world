class MyClass { //code to be run must be inside a class
  public static void main(String[ ] args) { //main method, static = method can be run without creating an instance of the class
                                            //void = no return value, parameters inside parentheses array of strings [args]
    System.out.println("Hello World. I remember you.");   //semicolons after code statements
  }
}
